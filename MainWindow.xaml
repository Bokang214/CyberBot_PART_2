<Window x:Class="CyberBot_PART_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛡️ Cybersecurity Awareness Bot" 
        Height="700" 
        Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117">

    <Window.Resources>
        <!-- Chat Bubble Styles —  -->
        <Style x:Key="UserBubble" TargetType="Border">
            <Setter Property="Background" Value="#1F6FEB"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="100,5,15,5"/>
        </Style>

        <Style x:Key="BotBubble" TargetType="Border">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="15,5,100,5"/>
        </Style>

        <!-- Modern Button Style —  -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#1F6FEB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#DA3633"/>
            <Setter Property="Width" Value="80"/>
        </Style>

        <!--  Quiz answer button style -->
        <Style x:Key="QuizButton" TargetType="Button">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#C9D1D9"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1C2128"/>
                                <Setter TargetName="bg" Property="BorderBrush" Value="#58A6FF"/>
                                <Setter Property="Foreground" Value="#58A6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Slim scrollbar -->
        <Style x:Key="SlimScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#30363D" CornerRadius="3" Margin="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

   
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- header -->
            <RowDefinition Height="*"/>
            <!-- chat -->
            <RowDefinition Height="Auto"/>
            <!-- quiz panel -->
            <RowDefinition Height="Auto"/>
            <!-- input -->
        </Grid.RowDefinitions>

        <!-- Header — ASCII art -->
        <Border Grid.Row="0" Background="#161B22" BorderBrush="#30363D" BorderThickness="0,0,0,1">
            <StackPanel Margin="20,15">
                <TextBlock FontFamily="Consolas" 
                           FontSize="9" 
                           Foreground="#58A6FF"
                           TextAlignment="Center"
                           FontWeight="Bold"
                           Text="╔════════════════════════════════════════════════════════════════════════════════╗&#x0a;║                                     ____      _               ____        _    ║&#x0a;║                                    / ___|   _| |__   ___ _ __| __ )  ___ | |_   ║&#x0a;║                                   | |  | | | | '_ \ / _ \ '__|  _ \ / _ \| __|  ║&#x0a;║                                   | |__| |_| | |_) |  __/ |  | |_) | (_) | |_   ║&#x0a;║                                    \____\__, |_.__/ \___|_|  |____/ \___/ \__|  ║&#x0a;║                                        |___/                                    ║&#x0a;║                                              Stay Safe Online!                  ║&#x0a;╚════════════════════════════════════════════════════════════════════════════════╝"/>
                <TextBlock Foreground="#8B949E" FontSize="12" TextAlignment="Center" Margin="0,8,0,0">
                    <Run Text="Cybersecurity Awareness Bot" Foreground="#58A6FF" FontWeight="Bold"/>
                    <Run Text="•"/>
                    <Run Text="Protecting You in the Digital World"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Chat scroll area -->
        <ScrollViewer x:Name="ChatScrollViewer" 
                      Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Background="#0D1117">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource SlimScrollBar}"/>
            </ScrollViewer.Resources>
            <StackPanel x:Name="ChatPanel" Margin="15"/>
        </ScrollViewer>

        <!-- Quiz panel (shown only during a quiz) -->
        <Border x:Name="QuizPanelBorder" Grid.Row="2"
                Background="#161B22"
                BorderBrush="#1F6FEB" BorderThickness="0,1,0,0"
                Padding="20,14" Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="QuizQuestionText"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="#C9D1D9"
                           TextWrapping="Wrap" Margin="0,0,0,10"/>
                <StackPanel x:Name="QuizOptionsPanel"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock x:Name="QuizProgressText"
                               FontSize="11" Foreground="#8B949E" VerticalAlignment="Center"/>
                    <TextBlock x:Name="QuizScoreText"
                               FontSize="11" Foreground="#2ECC71"
                               Margin="16,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Input area -->
        <Border Grid.Row="3" Background="#161B22" Padding="15"
                BorderBrush="#30363D" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="InputTextBox" 
                         Grid.Column="0"
                         Height="40"
                         FontSize="14"
                         Foreground="#C9D1D9"
                         Background="#0D1117"
                         BorderBrush="#30363D"
                         BorderThickness="1"
                         Padding="12,8"
                         CaretBrush="#58A6FF"
                         VerticalContentAlignment="Center"
                         TextWrapping="Wrap"
                         KeyDown="InputTextBox_KeyDown"/>

                <Button x:Name="SendButton" 
                        Grid.Column="1"
                        Style="{StaticResource ModernButton}"
                        Content="📤 Send"
                        Click="SendButton_Click"
                        Margin="10,0,0,0"/>

                <Button x:Name="VoiceButton" 
                        Grid.Column="2"
                        Style="{StaticResource ModernButton}"
                        Content="🔊 Voice"
                        Click="VoiceButton_Click"
                        Margin="5,0,0,0"/>

                <Button x:Name="ClearButton" 
                        Grid.Column="3"
                        Style="{StaticResource DangerButton}"
                        Content="🗑️ Clear"
                        Click="ClearButton_Click"
                        Margin="5,0,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
